# NEVER delete or change below line otherwise your crontab reload stops working!!!
* * * * * crontab /etc/cron.d/cronjobs && service cron reload
# Start writing your cronjobs after this line.
* * * * * echo "Hello world NGINX-PHP" >> /var/log/cron.log 2>&1
